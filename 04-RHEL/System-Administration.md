---
tags: [rhel]
status: stub
created: 2025-07-08
---
# System Administration
> Core Linux system administration tasks on RHEL.

## Topics
- User and group management (`useradd`, `usermod`, `groupadd`)
- File permissions and ACLs (`chmod`, `chown`, `setfacl`)
- Process management (`ps`, `top`, `kill`, `nice`)
- Scheduled tasks (`cron`, `at`, systemd timers)
- Log management (`journalctl`, rsyslog)
- SSH configuration and key management

## Related Notes
- [[Package-Management-DNF-RPM]], [[Systemd]], [[SELinux]]
- [[RHEL-SysAdmin-Path]] — Learning path
